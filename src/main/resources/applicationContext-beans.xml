<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans 
	http://www.springframework.org/schema/beans/spring-beans-4.1.xsd">
	<!-- <bean> 태그: - Spring에서 빈을 정의할 때 사용됩니다. - id 속성 : 빈의 고유 식별자로 사용됩니다. 다른 
		빈에서 이 식별자를 사용하여 의존성 주입(Dependency Injection)을 할 수 있습니다. - class 속성 : 빈이 생성될 
		클래스의 풀 패키지 경로를 지정합니다. -->
	<bean id="memberService"
		class="com.feb.jdbc.service.MemberService">
		<!-- <constructor-arg> 태그:- 생성자 인자를 설정할 때 사용됩니다. - ref 속성: 다른 빈을 참조하여 의존성을 
			주입할 때 사용됩니다. -->
		<constructor-arg ref="memberDao" />
		<!-- name ="타켓 변수명" 즉, MemberService(안에있는 변수명), ref="타겟 변수에 해당하는 클래스" value는 
			기본형이고 ref는 참조형이다. 근데 왜 name과 ref의 값ㅇㅣ 같은가? -->
		<property name="emailUtil" ref="emailUtil" />
	</bean>


	<bean id="memberDao" class="com.feb.jdbc.dao.impl.MemberDaoImpl">
		<property name="dataSource" ref="dataSource" />
	</bean>

	<bean id="noticeService"
		class="com.feb.jdbc.service.NoticeService">
		<constructor-arg ref="noticeDao" />
	</bean>

	<bean id="noticeDao" class="com.feb.jdbc.dao.impl.NoticeDaoImpl">
		<property name="dataSource" ref="dataSource" />
	</bean>

	<bean id="loginService" class="com.feb.jdbc.service.LoginService">
		<constructor-arg ref="loginDao" />
	</bean>

	<bean id="loginDao" class="com.feb.jdbc.dao.impl.LoginDaoImpl">
		<property name="dataSource" ref="dataSource" />
	</bean>

	<!-- EmailUtil email = new EamilUtill("mailSender"); 과 같고, bean id="emailUtil" 변수명이고 
		class="com.feb.util.EmailUtil" 클래스명이다 -->
	<bean id="emailUtil" class="com.feb.jdbc.util.EmailUtil">
	<constructor-arg ref="mailSender" />
	</bean>

	<!-- 이메일 전송하기 위한 bean설정 -->
	<bean id="mailSender" class="org.springframework.mail.javamail.JavaMailSenderImpl">
		<property name="host" value="smtp.naver.com" />
		<property name="port" value="587" />
		<property name="username" value="jbw02003" />
		<property name="password" value="159753sung!@" />
		<property name="javaMailProperties">
			<props>
				<prop key="mail.smtp.starttls.enable">true</prop>
			</props>
		</property>
	</bean>

</beans>
